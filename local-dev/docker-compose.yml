services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
      - "4000-4009:4000-4009"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certs:/etc/nginx/certs:ro
    restart: unless-stopped
